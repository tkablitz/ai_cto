# Phase <N> Roadmap — "<Theme>"

**Approved:** <date>. **Format:** one epic per working day; each epic gets its own
brainstorm → spec → plan → build cycle; this file's Status Tracker is the source of truth.
EOD handoff + memory update at each close.

## Mission

<2–4 sentences: what this phase makes true that wasn't true before, and why now.>

**Deferred to Phase <N+1> (its own theme, not passengers here):** <list>.
**Watch items (daily canary, raise-don't-fix):** <list>.

## Locked decisions (<date>)

1. <Theme + what was explicitly chosen over what.>
2. <Ordering decision and the reason (e.g., riskiest-first, toolchain-first).>
3. **Exit gate =** <binary, scriptable checklist — what "phase complete" means>.
4. <Standing pipeline/process contract that stays unchanged.>
5. Backlog source: <doc>; item numbers below refer to it.

## The epics

### Epic 1 — <name> (backlog <ref>, ⭐ if headline)
<Scope in 3–6 lines. **Folds in:** related small items. **Done =** observable, binary outcome.>

### Epic 2 — <name>
<…>

**Pre-decided input (<date>, owner-approved — fold into the Epic N brainstorm):** <decision
made early; scope sketch; gotchas already known; alternatives explicitly rejected with reasons.>

## Status Tracker (UPDATE AT EACH EOD)

- [ ] **Epic 1 — <name>** — <status; on completion: date, version/tag, pipeline run ID,
  what the deploy proved, suite counts, defects found-and-fixed on the way>
- [ ] **Epic 2 — <name>** — …
- [ ] **Exit gate** — <each box + evidence link when run>

<!-- SPDX-License-Identifier: MIT · © 2026 Torsten Kablitz · https://github.com/tkablitz/ai_cto -->
